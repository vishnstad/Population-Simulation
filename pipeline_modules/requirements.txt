# B17 pipeline dependencies.
# Python 3.11+

# --- core numerics / data --------------------------------------------
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scipy>=1.11
scikit-learn>=1.3

# --- config, templating, validation -----------------------------------
PyYAML>=6.0
Jinja2>=3.1
pydantic>=2.5

# --- LLM providers (install only the one you use; all are optional) ----
anthropic>=0.40        # primary
openai>=1.30           # cross-provider ablation
google-genai>=1.0      # cross-provider ablation

# --- app + figures -----------------------------------------------------
streamlit>=1.30
matplotlib>=3.8

# --- preprocessing (M1) ------------------------------------------------
pyreadstat>=1.2        # reads the GSS .dta

# --- tests -------------------------------------------------------------
pytest>=8.0
